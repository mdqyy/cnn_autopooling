function out = combinations(vec1, vec2, combnum)
%
% INPUTS
% vec1 : N x 1 vector
% vec2 : N x 1 vector
% combnum : number of 
%
% OUTPUTS
% out
%

out = nan(length(vec1)*length(vec2),2);

for m=1:length(vec2)
    for k=1:length(vec1)
        out((),:) = [k, m];
    end
end


end